一、LCD显示视频框架
	1、获取视频数据
	2、把视频数据转换为LCD能显示的数据
	3、在LCD上显示
	
二、V4L2框架：应用程序如何调用摄像头设备
1、打开摄像头设备
	接下来使用各种ioctl来获取属性等；主要如下过程：
2、使用VIDIOC_QUERYCAP:确定它是否视频捕捉设备,支持哪种接口(streaming/read,write)
3、使用VIDIOC_ENUM_FMT 查询支持哪种格式
4、使用VIDIOC_S_FMT 设置摄像头使用哪种格式
5、使用VIDIOC_REQBUFS  申请buffer
6、使用VIDIOC_QUERYBUF 确定每一个buffer的信息 并且 mmap
7、使用VIDIOC_QBUF  放入队列 
8、启动摄像头开始读数据 
9、使用poll来等待是否有数据
10、处理数据（把视频数据格式转换为LCD能显示的数据）
11、VIDIOC_QBUF 放入队列
12、停止摄像头

三、YUV格式
	“Y”表示明亮度（Luminance或Luma），也就是灰阶值；
	而“U”和“V” 表示的则是色度，作用是描述影像色彩及饱和度，用于指定像素的颜色。
	“亮度”是透过RGB输入信号来建立的，方法是将RGB信号的特定部分叠加到一起。
	“色度”则定义了颜色的两个方面─色调与饱和度，分别用Cr和CB来表示。
　YUV与RGB相互转换的公式如下（RGB取值范围均为0-255）︰ 
　　Y = 0.299R + 0.587G + 0.114B 
　　U = -0.147R - 0.289G + 0.436B 
　　V = 0.615R - 0.515G - 0.100B 
　　R = Y + 1.14V 
　　G = Y - 0.39U - 0.58V 
　　B = Y + 2.03U 

四、YUV转化为rgb
	在这里就不在一点点写，直接移植就行了。
